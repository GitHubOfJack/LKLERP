# AGENTS.md

## Cursor Cloud specific instructions

This repository is a blank project scaffold configured with the **OpenSpec** spec-driven development workflow for Cursor IDE. It contains no application code, dependencies, services, or build system.

### Repository contents

- `openspec/config.yaml` — OpenSpec configuration (schema: `spec-driven`). No tech stack or context defined yet.
- `.cursor/commands/opsx-*.md` — Cursor IDE command definitions for the OpenSpec workflow.
- `.cursor/skills/openspec-*/SKILL.md` — Cursor IDE skill definitions for the OpenSpec workflow.

### Development notes

- **No dependencies to install.** There is no `package.json`, `requirements.txt`, `pyproject.toml`, or any other dependency file.
- **No services to start.** There is no application, server, or database.
- **No lint, test, or build commands.** There are no configured linters, test frameworks, or build scripts.
- **No README.** Project documentation has not been created yet.
- When a tech stack is chosen and application code is added, this file should be updated with relevant setup, lint, test, and run instructions.
